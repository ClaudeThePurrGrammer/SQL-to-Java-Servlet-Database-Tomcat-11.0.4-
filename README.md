# SQL-to-Java-Servlet-Database-Tomcat-11.0.4-
A simple Java Servlet project using Tomcat 11 and MySQL. It demonstrates how to connect to a database with JDBC, run queries, and display results in HTML. Includes setup steps for Eclipse, servlet mapping, deployment configuration, and MySQL driver integration.
